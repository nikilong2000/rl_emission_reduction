\documentclass[tikz,border=10pt]{standalone}
\usepackage{tikz}
\usetikzlibrary{shapes.geometric, arrows.meta, positioning, fit, backgrounds, calc}

\begin{document}
% Define colours from Mermaid theme
\definecolor{primarybg}{HTML}{E6F3FF}
\definecolor{primaryborder}{HTML}{000000}
\definecolor{storebg}{HTML}{FEF9E7}
\definecolor{storeborder}{HTML}{B7950B}
\definecolor{phasebg}{HTML}{EEEEFF}
\definecolor{phaseborder}{HTML}{555577}

\begin{tikzpicture}[
    font=\sffamily\large,
    >=Latex,
    % Node Styles
    process/.style={rectangle, rounded corners=3pt, draw=primaryborder, fill=primarybg, thick, align=center, minimum height=1.2cm, inner sep=8pt},
    store/.style={cylinder, shape border rotate=90, aspect=0.25, draw=storeborder, fill=storebg, thick, align=center, minimum height=1.6cm, inner xsep=8pt, inner ysep=12pt},
    phasebox/.style={rectangle, rounded corners=8pt, draw=phaseborder, thick, fill=phasebg, fill opacity=0.4, dashed}
  ]

  % ---------------------------------------------------------
  % Nodes placement
  % Using explicit coordinates to perfectly control the columns
  % Left column: x=0. Right column: x=7.5. Center: x=3.75
  % ---------------------------------------------------------

  % Initial Store
  \node[store, text width=4.5cm] (A) at (0, 0) {Phase-1 best-seed\\checkpoint + vec\_normalize.pkl};

  % Phase 1 Node (S1)
  \node[process, text width=5cm] (B) at (7.5, 0) {reuse net + obs stats\\augment reward: +NOx +SOC};

  % Phase 2 Nodes (S2)
  \node[process, text width=4cm] (C) at (0, -4) {Variant A $\cdot$ 3$\times$3 grid\\9 cells};
  \node[process, text width=6cm] (D) at (7.5, -4) {Variant B $\cdot$ Optuna search\\continuous ranges $\cdot$ composite score J};
  \node[store, text width=3cm] (E) at (3.75, -6.5) {best weights};

  % Phase 3 Nodes (S3)
  \node[process, text width=5.5cm] (F) at (0, -10.5) {10-seed validation\\seeds 0--9 $\cdot$ 4M steps each\\train + evaluate};
  \node[process, text width=6cm] (G) at (7.5, -10.5) {Trade-off comparison vs Phase-1 baseline:\\speed RMSE $\times$ NOx $\times$ $\Delta$SOC};

  % ---------------------------------------------------------
  % Background Boxes for Subgraphs
  % ---------------------------------------------------------
  \begin{scope}[on background layer]
    % S1 Box
    \node[phasebox, fit=(B), inner ysep=25pt, inner xsep=15pt] (S1) {};
    \node[anchor=north, font=\bfseries, text=black, inner ysep=10pt] at (S1.north) {Warm-start fine-tune};

    % S2 Box
    \node[phasebox, fit=(C) (D) (E), inner ysep=25pt, inner xsep=15pt] (S2) {};
    \node[anchor=north, font=\bfseries, text=black, inner ysep=10pt] at (S2.north) {Reward-weight study};

    % S3 Box
    \node[phasebox, fit=(F) (G), inner ysep=25pt, inner xsep=15pt] (S3) {};
    \node[anchor=north, font=\bfseries, text=black, inner ysep=10pt] at (S3.north) {Multi-seed validation};
  \end{scope}

  % ---------------------------------------------------------
  % Edges
  % ---------------------------------------------------------

  % A to B
  \draw[->, thick] (A) -- (B);

  % B to C & D (Cross-Phase, routed through midpoints to avoid S2 title)
  \path (S1.south) -- (S2.north) coordinate[midway] (mid12);
  \draw[->, thick, rounded corners=5pt] (B.south) -- (B.south |- mid12) -| (C.north);
  \draw[->, thick, rounded corners=5pt] (B.south) -- (B.south |- mid12) -| (D.north);

  % C & D to E (Internal S2 routing)
  \draw[->, thick, rounded corners=5pt] (C.south) |- (E.west);
  \draw[->, thick, rounded corners=5pt] (D.south) |- (E.east);

  % E to F (Cross-Phase, routed through midpoints to avoid S3 title)
  \path (S2.south) -- (S3.north) coordinate[midway] (mid23);
  \draw[->, thick, rounded corners=5pt] (E.south) -- (E.south |- mid23) -| (F.north);

  % F to G
  \draw[->, thick] (F) -- (G);

\end{tikzpicture}
\end{document}
